namespace InheritLab
{
    interface IShuffle
    {
        // IShuffle should have 1 method --- void ShuffleCards(int times). “times” is the number of times you shuffle the deck.
        void ShuffleCards(int times);
    }
}
